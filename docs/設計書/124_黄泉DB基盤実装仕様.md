# 124 黄泉の階層 M1: DB・ランキング基盤 実装仕様

> ステータス: **実装仕様（未実装）** — [`YOMI_実装計画.md`](../progress/YOMI_実装計画.md) §2 M1（M1-1〜M1-5）の実装前仕様。実装担当は本書のみで作業可能な精度を目標に作成。
> 作成: 2026-07-04 / db-expert 調査・PM整理。行番号は 2026-07-04 時点の現行コードで全数検証済み（ズレなし）。

---

## 0. 前提訂正（着手前に必ず確認）

1. **現行 `prisma/schema.prisma` は11モデルのみ**（doc119 P5 まで実装済み）。`Account`/`Session`/`VerificationToken`/`Job`/`UserJob`/`WeaponMaterial`/`SpiritCore` は DROP 済み。`Character` は実質5列 + 3リレーションで、個人進行の唯一の真実は `Character.playerState`（JSONB = `PlayerSaveData`）。**新規の個人進行フィールドは playerState ブロブへ**（M1-5 のパターン）。`bestDungeonFloor` は横断集計用の例外として `PlayerStats` 正規化列に追加する（`bossKillCount` 等と同性質・doc122 §7.1）
2. YOMI_実装計画 M1 のファイル:行参照はすべて現行コードと一致確認済み（`schema.prisma:176` / `online.ts:1` / `route.ts:5` / `RankingService.ts:115/188/251-259`）
3. **`migratePlayerSaveSchema()` の実体は `src/services/PlayerSaveService.ts`（:258-322）**。`src/types/playerSave.ts` は定数と型のみ。実装ファイルを間違えないこと
4. `CH1_FINAL_NODE_ID = 'area1_node3'` は既存 `ABYSSAL_RESIDUE_UNLOCK_STAGE_ID`（`AbyssalResidueUnlockSystem.ts`）と同値・同意味（Ch1最終メインノード）だが、**意味的に別ゲートのため独立定数として新設**する。相互インポートはしない（値の重複はコメントで明記）

---

## 1. M1-1: `PlayerStats.bestDungeonFloor`（schema.prisma:176-183）

変更後モデル全文（`bestResidueScore` と `updatedAt` の間に1行追加のみ）:

```prisma
model PlayerStats {
  userId           String   @id
  user             User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  totalDamage      BigInt   @default(0)
  bossKillCount    Int      @default(0)
  bestResidueScore Float    @default(0)
  bestDungeonFloor Int      @default(0)
  updatedAt        DateTime @updatedAt
}
```

- `@@index` は付けない（既存の bossKillCount/bestResidueScore との対称性優先。重くなったら別途）
- 想定マイグレーション SQL: `ALTER TABLE "PlayerStats" ADD COLUMN "bestDungeonFloor" INTEGER NOT NULL DEFAULT 0;`（追加のみ・非破壊。doc119 P4 級のスナップショット必須要件はなし）

**Neon 適用手順**（doc31 §6/§8・doc122 §7.3 準拠）:
1. `.env.local` の `DATABASE_URL` が Neon **development ブランチ**であることを確認
2. `npx prisma validate`
3. `npx prisma migrate dev --name playerstats_best_dungeon_floor`
4. `npx prisma migrate status` で追従確認
5. 本番反映は Ch2 同梱デプロイ時: main ブランチ接続文字列で `npm run db:migrate:deploy` → その後 Pages 再デプロイ（doc31 §6 の順序厳守）

## 2. M1-2: 型・API

`src/types/online.ts:1`:

```diff
-export type RankingType = 'RESIDUE_SCORE' | 'STAGE_TIME' | 'TOTAL_DAMAGE' | 'BOSS_KILLS';
+export type RankingType = 'RESIDUE_SCORE' | 'STAGE_TIME' | 'TOTAL_DAMAGE' | 'BOSS_KILLS' | 'DUNGEON_FLOOR';
```

`src/app/api/ranking/route.ts:5`:

```diff
-const RANKING_TYPES: RankingType[] = ['RESIDUE_SCORE', 'STAGE_TIME', 'TOTAL_DAMAGE', 'BOSS_KILLS'];
+const RANKING_TYPES: RankingType[] = ['RESIDUE_SCORE', 'STAGE_TIME', 'TOTAL_DAMAGE', 'BOSS_KILLS', 'DUNGEON_FLOOR'];
```

⚠️ 追加漏れ時は `:10` のフォールバックで `RESIDUE_SCORE` に丸められる（サイレント失敗）。

## 3. M1-3: `RankingService.ts`

**(a) import 追加**（:3 の直後）:

```ts
import { getYomiFloorNumber } from '../logic/YomiFloors';
```

**(b) `getRanking` に分岐追加**（BOSS_KILLS ブロック :149-161 の直後・最終 else の直前。完全に同型）:

```ts
    } else if (type === 'DUNGEON_FLOOR') {
      const rows = await prisma.playerStats.findMany({
        orderBy: { bestDungeonFloor: 'desc' },
        take: limit,
        include: { user: true },
      });
      result = rank(rows.map(row => ({
        userId: row.userId,
        playerName: playerName(row.user),
        value: row.bestDungeonFloor,
        updatedAt: row.updatedAt.toISOString(),
      })));
    } else {
```

**(c) `recordStageClear` の upsert 直前**（:219-239。`becameTopResidue` 計算の後に2行追加、upsert の create/update 両方に `bestDungeonFloor` を追加）:

```ts
    // 黄泉の階層: getYomiFloorNumber は非黄泉ステージIDでは null を返すため、
    // currentStats はここで既に取得済みの値を再利用するだけで追加クエリは発生しない。
    const yomiFloor = getYomiFloorNumber(input.stageId);
    const nextBestDungeonFloor = Math.max(currentStats?.bestDungeonFloor ?? 0, yomiFloor ?? 0);
```

upsert 差分: `create` / `update` の両方に `bestDungeonFloor: nextBestDungeonFloor,` を追加。

- **非黄泉ステージで追加クエリゼロの根拠**: `currentStats` は既存コードが `bestResidueScore` 用に無条件1回 `findUnique` 済み。追加は純 JS 計算のみ。非黄泉では `yomiFloor === null` → 現状維持の no-op
- `return`（`StageRecordResult`）は変更不要（到達階の即時表示が欲しくなったら M3 で拡張検討）

**(d) `invalidate` にキー追加**（:251-259。BOSS_KILLS 行の直後）:

```ts
      ...limits.map(limit => `ranking:DUNGEON_FLOOR:global:${limit}`),
```

⚠️ 追加を忘れると DUNGEON_FLOOR のキャッシュが恒久的に無効化されない（実装計画 落とし穴7）。

## 4. M1-4: `src/logic/YomiFloors.ts`（新設・完全ソース）

```ts
/**
 * 黄泉の階層（無限ダンジョン）— ステージ ID / エリア判定の純関数集。
 *
 * 設計原則:
 *  - React / Prisma / Zustand に非依存の純関数のみ。
 *  - 黄泉判定はここに一元化する。呼び出し側で `stageId.startsWith('yomi_')` や
 *    `chapter === 1 && area === 99` を直接書かない（MAP混入防止・ランキング更新漏れ防止）。
 *  - 設計の正: docs/設計書/122_無限ダンジョン設計.md §1・§7 / docs/progress/YOMI_実装計画.md §1
 */

/** 黄泉ステージ ID の正規表現。`yomi_b01`〜`yomi_b20`（ゼロ埋め2桁）を想定するが、桁数・ゼロ埋めの有無は問わない。 */
export const YOMI_STAGE_ID_PATTERN = /^yomi_b(\d+)$/;

/** 黄泉の階層に割り当てる仮想 chapter/area（`areas.json` の `ch1_area99`「黄泉の階層」に対応）。 */
export const YOMI_CHAPTER = 1;
export const YOMI_AREA = 99;

/**
 * 黄泉の階層 B1 の `unlockRequires` が参照する「第1章・最終メインノード」ID。
 * U-1決定（2026-07-04）: Ch1クリア = このノードのクリア（`clearedStages` ベース）で黄泉タブを解放する。
 * `src/logic/AbyssalResidueUnlockSystem.ts` の `ABYSSAL_RESIDUE_UNLOCK_STAGE_ID` と同じ値（同じくCh1最終ノード）
 * だが意味的に別ゲートのため独立した定数として持つ。Ch1の背骨ステージ構成が変わった場合はこの1行だけ追随させる。
 */
export const CH1_FINAL_NODE_ID = 'area1_node3';

/** stageId が黄泉ステージ（`yomi_bNN` 形式）かどうかを判定する。 */
export function isYomiStage(stageId: string | null | undefined): boolean {
  if (!stageId) return false;
  return YOMI_STAGE_ID_PATTERN.test(stageId);
}

/**
 * 黄泉ステージ ID から階層番号（1始まりの整数）を導出する。
 * 黄泉ステージでない・数値化できない・0以下の場合は null を返す。
 */
export function getYomiFloorNumber(stageId: string | null | undefined): number | null {
  if (!stageId) return null;
  const match = YOMI_STAGE_ID_PATTERN.exec(stageId);
  if (!match) return null;
  const floor = Number.parseInt(match[1], 10);
  return Number.isFinite(floor) && floor > 0 ? floor : null;
}

/** chapter/area が黄泉の階層の仮想エリア（`ch1_area99`）かどうかを判定する。MAP除外フィルタ（M3-0）で使用。 */
export function isYomiArea(chapter: number | null | undefined, area: number | null | undefined): boolean {
  return chapter === YOMI_CHAPTER && area === YOMI_AREA;
}
```

**co-located `YomiFloors.test.ts` のケース一覧**（`AbyssalResidueUnlockSystem.test.ts` の流儀に合わせる）:
- `isYomiStage`: `yomi_b01`〜`yomi_b20`→true / `yomi_b1`・`yomi_b100`→true / 本編ID→false / `yomi_boss1`・`yomi_b`・`yomi_b1x`・`YOMI_B1`・前後空白→false / null・undefined・空文字→false
- `getYomiFloorNumber`: `yomi_b01`→1、`yomi_b09`→9、`yomi_b10`→10、`yomi_b20`→20 / `yomi_b1`→1、`yomi_b21`→21、`yomi_b100`→100 / **境界: `yomi_b0`・`yomi_b00`→null** / 非該当→null / null系→null
- `isYomiArea`: (1,99)→true / (1,1)・(2,99)・(0,99)→false / null/undefined 混在→false
- `CH1_FINAL_NODE_ID === 'area1_node3'` の凍結チェック（U-1決定）

## 5. M1-5: playerState v4

> ❌ **U-7 決定（2026-07-05）: 本節（M1-5）は実装しない**。節目チェスト=表示専用・世界ログ多重防止=`clearedStagesBeforeClear` 判定に確定し、`dungeonMilestonesClaimed` の参照箇所が消滅したため、playerState v4 昇格ごと見送り。以下は将来ワンショット演出フラグ等が必要になった場合の参照用アーカイブとして保持する。**実装担当は本節をスキップし、§6 の Step 4 も省略すること。**

### 5-1. `src/types/playerSave.ts`

- `PLAYER_SAVE_SCHEMA_VERSION = 3` → `4`
- `PlayerSaveSchemaVersion` の union に `| 3` を追加（前回 v2→v3 昇格時の流儀踏襲）
- `PlayerSaveData` 末尾（`transmutationPoints` の後）に追加:

```ts
  /** 黄泉の階層（無限ダンジョン）の節目チェスト（5/10/15/20階等）受取済フラグ。key は節目識別子、value は受取済みの階層番号配列。 */
  dungeonMilestonesClaimed: Record<string, number[]>;
```

### 5-2. `src/services/PlayerSaveService.ts`（**6箇所 + 任意1箇所**。漏れると tsc が落ちる）

**(a) 正規化関数の新設**（`normalizeNullableTuple` の後・:248 `PlayerSaveSchemaError` の前）:

```ts
export function normalizeDungeonMilestonesClaimed(value: unknown): Record<string, number[]> {
  if (!isRecord(value)) return {};
  const result: Record<string, number[]> = {};
  for (const [key, entry] of Object.entries(value)) {
    if (!Array.isArray(entry)) continue;
    const floors = Array.from(new Set(
      entry
        .map((floor) => Math.floor(Number(floor)))
        .filter((floor) => Number.isFinite(floor) && floor > 0),
    )).sort((a, b) => a - b);
    if (floors.length > 0) result[key] = floors;
  }
  return result;
}
```

**(b) `emptyPlayerSave()`**（:44-74）: `transmutationPoints: 0,` の後に `dungeonMilestonesClaimed: {},`

**(c) マイグレーションテーブル**（:258-284）に v3→v4 を追加:

```ts
  3: (state) => ({
    ...state,
    schemaVersion: 4,
    dungeonMilestonesClaimed: isRecord(state.dungeonMilestonesClaimed) ? state.dungeonMilestonesClaimed : {},
  }),
```

**(d) `readPlayerSave()`**（:324-353）末尾に追加:

```ts
    dungeonMilestonesClaimed: isRecord(migrated.dungeonMilestonesClaimed)
      ? normalizeDungeonMilestonesClaimed(migrated.dungeonMilestonesClaimed)
      : normalizeDungeonMilestonesClaimed(fallback.dungeonMilestonesClaimed),
```

**(e) `clonePlayerSave()`**（:355-374）:

```ts
    dungeonMilestonesClaimed: Object.fromEntries(
      Object.entries(save.dungeonMilestonesClaimed).map(([key, floors]) => [key, [...floors]]),
    ),
```

**(f) `playerSaveToJson()`**（:376-409）: `transmutationPoints` の後に `dungeonMilestonesClaimed: normalizeDungeonMilestonesClaimed(save.dungeonMilestonesClaimed),`

**(g)（任意・推奨）`hasCompletePlayerSave()`**（:411-429）に `&& isRecord(migrated.dungeonMilestonesClaimed)` を追加

### 5-3. スコープ外（M1 に含めない）

- `src/types/game.ts` の `CharacterData` / `toCharacterDataForSave()` への露出は **M3**（節目チェスト受取 UI）で
- `useGameStore` の `GAME_STORE_VERSION`（現在2）は**変更不要**（サーバ側 schemaVersion と独立のカウンタ）
- `cleanPlayerSaveReferencesWithIds`（:686-729）は変更不要（ID 参照を持たないため参照掃除対象外）

### 5-4. v3 セーブへの影響ゼロの確認観点

- 移行は純粋加算（`{}` 補完のみ）。既存フィールドは一切不変
- while ループ移行のため v1/v2 セーブも 1→2→3→4 と鎖状通過（テストは v3→v4 単体 + v1→v4 通しを推奨）
- DB 側の一括 schemaVersion スタンプは**行わない**（doc119 P5: shape が変わる昇格は app 側の遅延移行。初回アクセス時に `migratePlayerSaveSchema` が変換）

## 6. 実装順序とロールバック

| Step | 内容 | 検証 |
|---|---|---|
| 1. 型 | M1-2 + M1-4（YomiFloors + test） | `npx tsc --noEmit` / `npx jest src/logic/YomiFloors.test.ts` |
| 2. スキーマ | M1-1（Neon dev で migrate dev → status） | `prisma validate` → `migrate dev` → `migrate status` → `tsc`（PrismaClient 型に反映確認） |
| 3. サービス | M1-3（import/getRanking/recordStageClear/invalidate） | `tsc` / RankingService.test / route.test（§7-4） |
| ~~4. 移行~~ | ~~M1-5（playerSave.ts + PlayerSaveService.ts）~~ **省略（U-7 決定により削除）** | — |
| 最終 | 全体 | `tsc` → `npx jest --ci --silent --testPathIgnorePatterns="<rootDir>/src/tests/"`（CI同一）→ `npm run data:audit` |

依存: Step3 は Step1（YomiFloors）と Step2（Prisma 型）の両方に依存。Step4 は独立だが指定順で最後。

**ロールバック**:
- Step1: 新規ファイル削除 / `git checkout` のみ（波及なし）
- Step2: dev 適用済みなら `ALTER TABLE ... DROP COLUMN` + `prisma migrate resolve --rolled-back`、または Neon dev ブランチ作り直し（最safe）。**本番 deploy 済みならマイグレーションファイルを直接編集・削除せず、打ち消しマイグレーションを前方適用**
- Step3: 差分3ブロックを戻すだけ。Redis 残骸は TTL 300秒で自然失効
- Step4: ⚠️ v4 セーブが1件でも書き込まれた後に `PLAYER_SAVE_SCHEMA_VERSION` を3へ戻すと該当セーブが「未来バージョン」`PlayerSaveSchemaError` になる。**v4 データ未書込を確認してから**戻すこと

## 7. テスト計画

1. **`src/logic/YomiFloors.test.ts`**（新設・CI対象）— §4 のケース全数
2. ~~`PlayerSaveService.test.ts` の v4 拡張~~ — **不要（U-7 決定により M1-5 削除）**
3. `RankingService.test.ts` は変更不要（純関数のみテストの既存方針）
4. **（推奨・新設）`src/app/api/ranking/route.test.ts`** — logout/route.test.ts と同じ `jest.mock` パターンで CI 対象化: 不正 type → RESIDUE_SCORE フォールバック / `type=DUNGEON_FLOOR&limit=10` の透過
5. **（推奨・CI除外）`src/tests/yomi-dungeon-floor-ranking.integration.test.ts`** — 実DB: 非黄泉クリアで 0 のまま / `yomi_b05`→5（create 分岐）/ `yomi_b03`→5 のまま（不可逆・D-7）/ `yomi_b10`→10 / getRanking 降順 / invalidate のキー削除（Upstash 未設定で no-op 完走）

## 変更履歴

| 日付 | 内容 |
|---|---|
| 2026-07-04 | 初版（db-expert 調査。未実装・適用は YOMI 実装計画 M1） |
| 2026-07-05 | U-7 決定を反映: M1-5（playerState v4 / dungeonMilestonesClaimed）を実装対象から削除し §5 をアーカイブ化。M1 の実装範囲は M1-1〜M1-4 のみに確定 |
