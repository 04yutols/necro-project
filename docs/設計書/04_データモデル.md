# 04 — データモデル設計書

> 最終更新: 2026-05-06  
> 参照: `docs/TDD.md` (内容を本書に吸収)  
> 実装ファイル: `prisma/schema.prisma`, `src/types/game.ts`, `src/data/master/`

## 1. Prisma スキーマ（最新版）

```prisma
// prisma/schema.prisma

model User {
  id              String      @id @default(uuid())
  name            String
  characters      Character[]
  discoveredItems Item[]      @relation("Discoverer")
}

model Character {
  id              String   @id @default(uuid())
  name            String
  userId          String?
  user            User?    @relation(fields: [userId], references: [id])

  // 8種の基礎ステータス（HSR互換）
  hp        Int
  atk       Int
  def       Int
  spd       Int
  critRate  Float @default(5.0)
  critDmg   Float @default(150.0)
  effectHit Float @default(0.0)
  effectRes Float @default(0.0)

  // 永続パッシブ補正 (GDD-004) — 転職後もリセットされない
  passiveAtkBonus      Int   @default(0)
  passiveDefBonus      Int   @default(0)
  passiveSpdBonus      Int   @default(0)
  passiveCritRateBonus Float @default(0)
  passiveCritDmgBonus  Float @default(0)
  passiveHpBonus       Int   @default(0)

  // 職業システム (GDD-004)
  currentJobId    String?
  jobs            UserJob[]

  // 8枠装備スロット (GDD-007)
  equipWeaponId, equipSubId, equipHeadId, equipBodyId,
  equipArmsId, equipLegsId, equipAcc1Id, equipAcc2Id: String?
  // → Item モデルへの @relation (8本)

  // 死霊術ステータス (GDD-005)
  necroLevel          Int   @default(1)   // 1〜99
  necroRank           Int   @default(1)   // 1〜10
  necroMaxCost        Int   @default(10)  // パーティ最大コスト
  necroBaseStatsBonus Float @default(1.0) // ランクアップ倍率

  // 進行管理
  clearedStages   String[] @default([])

  // 深淵の残滓
  abyssalResidues AbyssalResidue[]
}

model Job {
  id       String    @id   // "warrior" など
  name     String
  tier     Int             // 1 or 2
  category String          // "PHYSICAL" | "MAGICAL"
  userJobs UserJob[]
}

// 職業ごとの独立レベル管理 — 転職してもこのレコードは残る (GDD-004)
model UserJob {
  characterId String
  jobId       String
  level       Int    @default(1)
  exp         Int    @default(0)
  character   Character @relation(fields: [characterId], references: [id])
  job         Job       @relation(fields: [jobId], references: [id])
  @@id([characterId, jobId])
}

model Item {
  id            String   @id @default(uuid())
  name          String
  type          String   // "WEAPON"|"SUB"|"HEAD"|"BODY"|"ARMS"|"LEGS"|"ACC1"|"ACC2"
  rarity        String   // "COMMON"|"UNIQUE"|"HIDDEN_UNIQUE"
  atk, def, spd: Int @default(0)
  critRate, critDmg: Float @default(0.0)
  specialEffect String?

  // 第一発見者システム (GDD-追加要件)
  isUnique        Boolean   @default(false)
  discovererId    String?
  discovererName  String?
  discoverer      User?     @relation("Discoverer", fields: [discovererId], references: [id])
  serialNo        Int?      // グローバル発見順シーケンス
  discoveredAt    DateTime?

  // ランダムオプション (JSONB)
  subOptions      Json?     // Array<{ type: string, value: number }>

  residues        AbyssalResidue[]
  // + 8本の equippedXxx リレーション（逆参照）
}

model Monster {
  id       String  @id @default(uuid())
  name     String
  tribe    String  // "UNDEAD"|"DEMON"|"BEAST"|"HUMANOID"
  cost     Int
  // 8種ステータス（HSR互換）
  hp, atk, def, spd: Int
  critRate, critDmg, effectHit, effectRes: Float

  soulShardId  String?
  soulShard    SoulShard?  @relation(fields: [soulShardId], references: [id])
  spiritCoreId String?
  spiritCore   SpiritCore? @relation(fields: [spiritCoreId], references: [id])
}

model SoulShard {
  id              String    @id @default(uuid())
  originMonster   String    // 変換元モンスター名
  atkBonus        Int       @default(0)
  elementDmgBoost Int       @default(0)
  specialAbility  String?   // "REGENERATE_SOUL"|"BANE_OF_LIGHT" など
  monsters        Monster[]
}

model SpiritCore {
  id           String    @id @default(uuid())
  name         String
  element      String?   // ElementType
  skillChangeId String?
  atkMultiplier Float    @default(1.0)
  monsters     Monster[]
}

model AbyssalResidue {
  id          String     @id @default(uuid())
  name        String
  itemId      String
  item        Item       @relation(fields: [itemId], references: [id])
  characterId String?
  character   Character? @relation(fields: [characterId], references: [id])
  subOptions  Json       // Array<{ type: string, value: number }>
  level       Int        @default(1)  // 1〜20
}
```

---

## 2. TypeScript 型定義（src/types/game.ts 全リスト）

```typescript
// ユニオン型
type ClassCategory = 'PHYSICAL' | 'MAGICAL'
type Tribe = 'UNDEAD' | 'DEMON' | 'BEAST' | 'HUMANOID'
type ElementType = 'FIRE'|'WATER'|'THUNDER'|'EARTH'|'WIND'|'LIGHT'|'DARK'|'ICE'|'NONE'
type SkillAttackType = 'SLASH'|'STRIKE'|'PROJECTILE'|'MAGIC'|'SUMMON'|'HEAL'

// ステータス
interface BaseStats {
  hp: number; atk: number; def: number; spd: number;
  critRate: number; critDmg: number; effectHit: number; effectRes: number;
}
type Resistances = Partial<Record<ElementType, number>>

// キャラクター
interface PassiveBonuses {
  passiveAtkBonus: number; passiveDefBonus: number; passiveSpdBonus: number;
  passiveCritRateBonus: number; passiveCritDmgBonus: number; passiveHpBonus: number;
}
interface UserJobState { jobId: string; level: number; exp: number }
interface SubOption { type: string; value: number }

interface EquipmentSlots {
  weapon,sub,head,body,arms,legs,acc1,acc2: ItemData | null
}

interface CharacterData {
  id: string; name: string; currentJobId: string; category: ClassCategory;
  stats: BaseStats; passives: PassiveBonuses; equipment: EquipmentSlots;
  baseResistances: Resistances; jobs: UserJobState[];
  isAwakened: boolean; clearedStages: string[];
  currentEnergy: number; maxEnergy: number;
  elementDmgBoosts: Partial<Record<ElementType, number>>;
}

// アイテム
interface ItemData {
  id: string; name: string;
  type: 'WEAPON'|'SUB'|'HEAD'|'BODY'|'ARMS'|'LEGS'|'ACC1'|'ACC2';
  rarity: 'COMMON'|'UNIQUE'|'HIDDEN_UNIQUE';
  stats: Partial<BaseStats>; resistances?: Resistances;
  specialEffect?: string; isUnique: boolean;
  discovererId?: string; discovererName?: string;
  serialNo?: number; discoveredAt?: string;
  subOptions?: SubOption[];
}

// モンスター・死霊術
interface SpiritCoreData {
  id: string; name: string; element?: ElementType;
  skillChangeId?: string; atkMultiplier: number;
}

interface MonsterData {
  id: string; name: string; tribe: Tribe; cost: number;
  stats: BaseStats; resistances: Resistances;
  equippedShardId?: string; spiritCore?: SpiritCoreData;
}

interface SoulShardData {
  id: string; originMonsterName: string;
  effect: { atkBonus: number; elementDmgBoost: number; specialAbility?: string };
}

interface NecroStatus {
  level: number;  // max 99
  rank: number;   // max 10
  maxCost: number;
  baseStatsBonus: number;  // 初期1.0、RankUpごと+0.5
}

// 深淵の残滓
interface AbyssalResidueData {
  id: string; name: string; itemId: string;
  rarity: 'COMMON'|'RARE'|'EPIC';
  mainStat: { type: string; value: number };
  subOptions: SubOption[];
  level: number; exp: number; maxExp: number;
}

interface ResidueMatData {
  id: string; name: string;
  quantity: number; expValue: number;
  rarity: 'COMMON'|'RARE'|'EPIC';
}

// バトル
interface SkillData {
  id: string; name: string; mpCost: number; power: number;
  type: 'PHYSICAL'|'MAGICAL'|'HEAL'; element?: ElementType;
  attackType?: SkillAttackType;
  targetType?: 'SINGLE'|'ALL_ENEMIES'|'SELF'|'ALLY';
  effectKey?: string; description: string;
}

interface BattleState {
  player: CharacterData; monsters: (MonsterData | null)[];
  wave: number; turn: number;
  areaGimmick?: 'SLIP_DAMAGE'|'STATUS_AILMENT'|'NONE';
}

interface BattleLog {
  turn: number; wave: number; action: string;
  actorName: string; targetName: string;
  damage?: number; isCritical?: boolean;
  isWeakness?: boolean; isResisted?: boolean;
  element: ElementType; attackType: SkillAttackType;
  playerEnergy: number; playerMP?: number; playerHP: number; description: string;
}
```

---

## 3. マスターデータ JSON スキーマ

### jobs.json
```json
{
  "[jobId]": {
    "name": "string",
    "tier": 1,
    "category": "PHYSICAL | MAGICAL",
    "energyCurve": { "baseMaxEnergy": 100, "energyRegen": 15, "ultimateCost": 100 },
    "levelBonuses": {
      "10": { "passiveAtkBonus": 5 }
    },
    "skills": [{ "level": 1, "skillId": "skill_xxx" }]
  }
}
```

### monsters.json
```json
{
  "[monsterId]": {
    "name": "string", "tribe": "UNDEAD|DEMON|BEAST|HUMANOID",
    "cost": 1,
    "stats": {
      "hp": 50, "atk": 10, "def": 5, "spd": 100,
      "critRate": 5, "critDmg": 150, "effectHit": 0, "effectRes": 0
    },
    "resistances": { "FIRE": -20 }
  }
}
```

### stages.json
```json
{
  "[stageId]": {
    "name": "string", "area": 1, "waves": 2,
    "enemies": [["goblin"], ["goblin","goblin"]],
    "rewards": {
      "baseExp": 50, "baseGold": 100,
      "dropTable": [{ "itemId": "iron_sword", "rate": 0.05 }]
    },
    "isAreaBoss": false,
    "position": { "x": -150, "y": 150 }
  }
}
```

### skills.json
```json
{
  "[skillId]": {
    "id": "string", "name": "string",
    "mpCost": 5, "power": 1.5,
    "type": "PHYSICAL|MAGICAL|HEAL",
    "element": "FIRE|...",
    "attackType": "SLASH|...",
    "targetType": "SINGLE|ALL_ENEMIES|SELF|ALLY",
    "effectKey": "fire_magic",
    "description": "string"
  }
}
```

---

`SkillData.mpCost` は旧名称を維持しているが、ゲーム内表示と実際の消費リソースは Energy。

---

## 4. ステータス最終計算式

```
finalStats = JobStats + PassiveBonuses + Equipment + AbyssalResidue
elementDmgBoosts = Equipment属性加成 + AbyssalResidue属性加成
```

1. `JobStats`: 現在職業・職業Lvに応じた `CharacterData.stats`
2. `PassiveBonuses`: 職業レベルマイルストーンで累積し、転職しても残る永続補正
3. `Equipment`: 8スロットの `ItemData.stats` と `subOptions`
4. `AbyssalResidue`: 装備中の深淵の残滓 `mainStat/subOptions`

```typescript
const profile = calculateCharacterStatProfile(player, equippedResidueSlots);
profile.total;            // hp / atk / def / spd / critRate / critDmg / effectHit / effectRes
profile.elementDmgBoosts; // 属性ダメージ加成
```

---

## 5. 報酬計算アルゴリズム

### EXP計算（RewardService.calculateExp）
```
gainedExp = baseExp × (1 + jobLevel/100) × categoryMultiplier
categoryMultiplier: MAGICAL=1.1 / PHYSICAL=1.0
```

### ドロップ率計算（RewardService.calculateDrops）
```
adjustedRate = baseRate × (1 + discoveryBonusRate/100)
drop = Math.random() < adjustedRate
```

`LUCK` は廃止済み。ドロップ率上昇は将来 `discoveryBonusRate` として、装備効果・祝福・イベント補正などから独立計算する。

---

## 6. 深淵の残滓（AbyssalResidue）成長式

```typescript
// upgradeResidue() 内のロジック
let newExp = residue.exp + gainedExp;
let newLevel = residue.level;
let newMaxExp = residue.maxExp;

while (newExp >= newMaxExp && newLevel < 20) {
  newExp -= newMaxExp;
  newLevel++;
  newMaxExp = Math.floor(newMaxExp * 1.5);  // 1.5倍ずつ必要EXP増加
}
```

初期 maxExp の例: Lv1=800, Lv2=1200, Lv3=1800, ... Lv20=上限

---

## 7. 第一発見者システム DB 設計

```
HIDDEN_UNIQUE アイテムがドロップ
  → PostgreSQL グローバルシーケンスで serialNo を発番
  → Item.isUnique = true
  → Item.discovererId = User.id
  → Item.discovererName = User.name
  → Item.discoveredAt = 現在時刻
  → 以降そのアイテムを取得した全プレイヤーに discovererName が表示される
```

**設計意図**: `serialNo` はグローバル採番なので「世界で何番目に見つけた」が分かる。`isUnique=true` かつ `discovererId` が存在する場合のみ鑑定証明書の生成が可能。
