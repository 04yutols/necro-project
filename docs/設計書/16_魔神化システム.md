# 16 — 魔神化（Demonization）システム設計書

> 最終更新: 2026-05-08  
> 関連設計書: [03_バトルシステム.md](03_バトルシステム.md)（§6 に概要あり）, [10_職業転職システム.md](10_職業転職システム.md)  
> 実装ファイル: `src/store/useGameStore.ts`（demonGauge / isDemonMode）, `src/components/battle/BattleCanvas.tsx`  
> 将来の実装ファイル（予定）: `src/logic/DemonizationSystem.ts`, `src/data/master/demonForms.json`

---

## 1. コアコンセプト

主人公アルドは表向き「勇者の遺産（各職業）」を装備して戦うが、
ソウルゲージがMAXになった時、根底にある**「魔族の力（死霊術師）」**をその職業に注ぎ込み、
光の力を反転・暴走させる。これを「**魔神化（Demonization）**」と呼ぶ。

> **世界観補足**: 「剣士の魔神化」は剣聖が闇に堕ちた姿であり、「メイジの魔神化」は秩序の魔法が
> 死霊の呪いに侵食された姿。職業の「正の力」が「負の力」に反転する瞬間がビジュアルで表現される。

---

## 2. 基本システム仕様（共通）

### 2.1 現行実装（`useGameStore.ts`）

```typescript
// 現在の実装ステート
demonGauge:   number;   // 0〜100
isDemonMode:  boolean;

// 現在の動作
fillDemonGauge(amount: number)  // 0-100にクランプ済み
toggleDemonMode()               // isDemonMode の on/off
```

| 要素 | 現行仕様 |
|---|---|
| ゲージ蓄積 | 通常: +10/ターン |
| 発動条件 | `demonGauge >= 100 && !isDemonMode` |
| 持続 | 3回行動するまで |
| 消費 | 発動中: -20/ターン |
| 効果 | ダメージ +20%、専用スキル3種、アリーナが赤くなる |
| 解除 | 3ターン経過 または `toggleDemonMode()` |

### 2.2 拡張仕様（将来実装）

現行の「一律+20%」を廃止し、**装備中の職業ごとに固有の魔神化フォーム**を適用する。

| 追加要素 | 詳細 |
|---|---|
| **発動（絶対割り込み）** | ゲージMAX時に任意発動。発動瞬間にプレイヤーのAV（行動値）= 0 になり、敵の行動をキャンセルして最前列に割り込む |
| **持続** | 主人公が**3回行動するまで**（ターン数ではなく行動数基準）|
| **状態異常無効** | 魔神化中、全ての状態異常・デバフを完全無効化 |
| **魔神技（Ultimate Skill）** | 魔神化中に1度だけ使用可能な専用の奥義 |
| **Effect A（基礎昇華）** | 常時発動する暴力的なステータスバフ |
| **Effect B（深淵の理）** | 職業・Tierに応じたギミックと代償（Tier2は強烈なリスク必須） |

---

## 3. Tier別 魔神化「尖り（Peaky）」設計ルール

### Tier 1（初期職: 剣士・盗賊・メイジ・闇術師等）

| 項目 | ルール |
|---|---|
| **設計方針** | 分かりやすく強力。純粋なステータスバフ＋扱いやすいギミック |
| **リスク** | デメリットは設定しなくてよい |
| **魔神技** | 単純な大ダメージ、汎用性の高い自己バフ、フィールド生成 |
| **バランス感** | 初見でも「強い！」と感じられる爽快感を優先 |

### Tier 2（上位職: ベルセルク・アークメイジ等）

| 項目 | ルール |
|---|---|
| **設計方針** | 前提の2つの下位職の要素を**悪魔合体**させる |
| **リスク（必須）** | 強烈なクセ（代償）をEffect Bに必ず組み込む |
| **魔神技** | 絶大ダメージ＋戦闘ローテーションを有利にする**持続効果を必ず残す** |
| **禁止事項** | 単純な火力インフレ（HP1で致死ダメージ等）。ターン制RPGの戦略を破壊するため厳禁 |

**Tier 2 で推奨されるリスクパターン**:

| リスク名 | 効果 | パーティへの影響 |
|---|---|---|
| **自傷** | 攻撃するたびに味方全体HPを削る | ヒーラー必須化 |
| **MP過剰消費** | 魔法が連射できるがエネルギー消費数倍 | バッテリー役必須化 |
| **紙装甲** | 火力青天井だが被ダメ 2〜3倍 | タンク・挑発キャラ必須化 |
| **セットアップ依存** | 基礎火力ゼロ＋敵のデバフ数だけ乗算 | デバフコンボ必須化 |

---

## 4. 魔神化フォーム サンプルカタログ（学習用データ）

### 4.1 アークメイジ（Tier 2）の魔神化

| 要素 | 内容 |
|---|---|
| **魔神化名** | 『虚無の暴君』 |
| **コンセプト** | 闇術師の「命の搾取」を取り入れた、味方の命を燃やす固定砲台 |
| **Effect A** | 魔法ダメージ +150%。敵の属性耐性を完全無視する |
| **Effect B** | 魔法スキルを撃つたびに、主人公以外の味方全員の現在HP 20% を吸収（自傷） |
| **魔神技** | 『カタストロフィ・ゼロ』 |
| ↳ 演出&効果 | 画面全域を無に帰す極大魔法ダメージ |
| ↳ 残留効果 | フィールドに「味方の魔法火力が上がり続けるが、ターン終了時に味方全体がスリップダメージを受ける」呪いの領域を残す |

---

### 4.2 ベルセルク（Tier 2）の魔神化

| 要素 | 内容 |
|---|---|
| **魔神化名** | 『血河の狂戦士（ブラッド・レイジ）』 |
| **コンセプト** | 盗賊の手数が「反動ダメージ」を伴って暴走した連撃特化 |
| **Effect A** | 基礎攻撃力 +150% |
| **Effect B** | すべての物理攻撃が確定3回ヒットに分裂するが、魔神化中の被ダメージが常時 2倍（紙装甲化） |
| **魔神技** | 『絶禍の断頭台』 |
| ↳ 演出&効果 | 敵単体を粉砕する防御無視ダメージ |
| ↳ 残留効果 | 味方全体に「攻撃して与えたダメージの10%分HPを回復する（吸血）」バフを付与し、主人公の紙装甲をパーティ全体でカバーさせる |

---

## 5. 魔神化フォームのマスターデータ構造（将来実装時）

```typescript
// src/data/master/demonForms.json — 職業ごとのフォーム定義
interface DemonForm {
  jobId: string;                  // 対応する職業ID（"berserker" 等）
  formName: string;               // 魔神化名（例: "血河の狂戦士"）
  tier: 1 | 2;
  concept: string;
  effectA: {
    descJa: string;               // 表示用テキスト
    statBoosts: Partial<Record<keyof BaseStats, number>>;  // 数値バフ
    flags?: string[];             // 特殊フラグ（例: "IGNORE_RESISTANCE"）
  };
  effectB: {
    descJa: string;
    riskType: 'SELF_DAMAGE' | 'ENERGY_DRAIN' | 'GLASS_CANNON' | 'SETUP_DEPENDENT' | null;
    riskValue?: number;           // Tier1はnull
    onAttackEffect?: string;      // 攻撃時トリガーのフラグ（例: "TRIPLE_HIT"）
  };
  ultimateSkill: {
    nameJa: string;
    damage: {
      power: number;              // SkillData.power と同形式
      element: ElementType;
      targetType: 'SINGLE' | 'ALL';
      flags?: string[];           // 例: "IGNORE_DEF"
    };
    lingering: {
      type: 'FIELD' | 'PARTY_BUFF' | 'ENEMY_DEBUFF';
      descJa: string;
      duration: number;           // ターン数（-1=バトル終了まで）
    };
  };
}
```

**`demonForms.json` サンプルエントリ（ベルセルク）**:

```json
{
  "jobId": "berserker",
  "formName": "血河の狂戦士",
  "tier": 2,
  "concept": "盗賊の手数が反動ダメージを伴って暴走した連撃特化",
  "effectA": {
    "descJa": "基礎攻撃力が+150%される",
    "statBoosts": { "atk": 1.5 },
    "flags": []
  },
  "effectB": {
    "descJa": "全物理攻撃が確定3回ヒットに分裂するが、被ダメージが常時2倍になる",
    "riskType": "GLASS_CANNON",
    "riskValue": 2.0,
    "onAttackEffect": "TRIPLE_HIT"
  },
  "ultimateSkill": {
    "nameJa": "絶禍の断頭台",
    "damage": {
      "power": 4.5,
      "element": "DARK",
      "targetType": "SINGLE",
      "flags": ["IGNORE_DEF"]
    },
    "lingering": {
      "type": "PARTY_BUFF",
      "descJa": "与ダメージの10%吸血バフを味方全体に付与",
      "duration": 3
    }
  }
}
```

---

## 6. BattleEngine への統合（将来実装）

```typescript
// src/logic/BattleEngine.ts — 魔神化発動時の処理フロー
private processDemonization(player: CharacterData): void {
  const form = this.masterData.getDemonForm(player.currentJobId);
  if (!form) return;

  // 1. 絶対割り込み: AVを0に設定
  player.currentAV = 0;

  // 2. Effect A: 基礎ステータスバフ適用
  this.applyDemonEffectA(player, form.effectA);

  // 3. 状態異常を全解除
  player.statusEffects = [];

  // 4. 魔神化フラグ立て
  player.isDemonMode = true;
  player.demonActionsRemaining = 3;

  this.addLog('DEMON_ACTIVATE', player.name, '', `魔神化『${form.formName}』発動！`);
}

private applyDemonEffectB(player: CharacterData, form: DemonForm): void {
  // 攻撃後トリガーとして登録（例: TRIPLE_HIT, SELF_DAMAGE）
  player.demonEffectBFlag = form.effectB.onAttackEffect ?? null;
  player.demonRiskType    = form.effectB.riskType;
  player.demonRiskValue   = form.effectB.riskValue ?? 0;
}
```

---

## 7. AI生成プロンプトフォーマット（設計ガイド）

新しい職業の魔神化を設計する際は、エージェントスキル `/design-demonform` を使用する。
スキルの定義は `/Users/yuto/.claude/agents/demonization-designer.md` を参照。

手動でプロンプトを送る場合は以下のフォーマットを使用すること:

```
# Instruction
あなたはAAA級ダークファンタジーRPG『ネクロマンスブレイブ』の専属バトルプランナーです。
ガイドラインの「Tier別：魔神化の尖り設計ルール」に従い、以下の職業に対する
【魔神化フォーム】と【魔神技】を設計し、指定のMarkdownフォーマットで出力してください。

# Target Job
- 職業名: [例：アサシン]
- Tier: [1 or 2]
- 職業の特徴: [例：手数と回避に特化。盗賊と剣士の派生]

# Output Format (Markdown)
### 職業名: [職業名] の魔神化
- **魔神化名:** 『[中二病的なダークファンタジー感のある二つ名]』
- **コンセプト:** [プレイスタイルとリスクの簡潔な説明]
- **Effect A (基礎昇華):** [基礎ステータスの暴力的な向上]
- **Effect B (深淵の理):** [Tierに応じたギミックと強烈なリスク・代償]
- **魔神技 (Ultimate Skill): 『[技名]』**
  - **演出&効果:** [ダメージの性質]
  - **残留効果:** [その後の戦闘を有利にするフィールド・バフ・デバフ]
```

---

## 8. 将来の実装チェックリスト

- [ ] `src/data/master/demonForms.json` — 全12職業分のフォーム定義
- [ ] `src/logic/DemonizationSystem.ts` — AV割り込み・Effect A/B適用・状態異常解除・行動数カウント
- [ ] `src/logic/BattleEngine.ts` — 魔神技（Ultimate Skill）ダメージ計算・残留効果適用
- [ ] `src/store/useGameStore.ts` — `demonActionsRemaining`, `demonEffectBFlag` フィールド追加
- [ ] UI: 魔神化発動VFX（ジョブごとに異なる色彩・エフェクト）
- [ ] UI: ソウルゲージUI（満タン時の脈動アニメーション → 発動ボタン出現）
- [ ] UI: 魔神技専用ボタン（魔神化中のみ出現、使用後グレーアウト）
- [ ] テスト: `DemonizationSystem.test.ts` — AV割り込み優先度・Effect B自傷ダメージ・状態異常無効の検証
