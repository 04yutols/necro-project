# 101 — AI 草案エージェント拡張ロードマップ

> 作成: 2026-06-08
> 前提: [100_管理画面AIエージェント設計.md](100_管理画面AIエージェント設計.md) で Agent A（エネミー）実装済み
> 目的: 管理画面で編集できる全コンテンツに「AI 草案」を展開するための対象一覧と優先度

---

## 1. 方針（Agent A で確立したパターンの横展開）

各コンテンツで以下の 3 点セットを再利用する:

1. **素性付きコンテキスト**: 既存データ + 参照可能 ID + 設計指針（設計書 or 実データから学習）をプロンプトへ
2. **決定論的バリデータ**（純関数）: 構造・参照整合・数値スケール・帯逸脱を機械判定（PASS/WARN/FAIL）
3. **生成→検証→FAILなら再生成** ループ（LangGraph）+ フォーム流し込み（保存は既存 Server Action）

`gemini.ts`（クライアント・リトライ）と `thinkingBudget.ts`（動的予算）はそのまま共通利用できる。

---

## 2. 対象コンテンツ一覧（管理画面の全 SECTION + ストーリー）

| # | コンテンツ | 件数 | 状態 | 優先度 | 決定論的ゲートの根拠 |
|---|---|---:|---|---|---|
| 1 | **enemies** 敵 | 10 | ✅ 実装済 | — | 実データ tier 帯 + necromance 規約 |
| 2 | **skills** スキル | 29 | ✅ 実装済 | — | **19_スキルバランス設計書**（cost帯×power表）+ 紐付き先 owner-fit |
| 3 | **demonForms** 魔神化 | 12 | ✅ 実装済 | — | 16_魔神化（Tierルール + 魔神技 power 基準）+ 職業紐付き |
| 4 | **stages** ステージ | 6 | ✅ 実装済 | — | 参照整合（enemyIds/unlock/area/drop）+ 進行/報酬カーブ |
| 5 | **monsters** 味方魔物 | 9 | 未 | 🟡 中 | enemies の味方検証を流用（cost×stats）|
| 6 | **items** 武器 | 10 | ✅ 実装済 | — | 13_武器システム + 92_IMP11（rarity別 subOption枠 / passive 2スロット）|
| 7 | **materials** 素材 | 7 | 未 | 🟢 低（簡単） | rarity 列挙 + expValue 帯 |
| 8 | **jobs** 職業 | 12 | 未 | 🟢 低（低頻度） | 10_職業転職（statModifiers 倍率）|
| 9 | **areas** エリア | 2 | 未 | 🟢 低 | メタデータのみ（chapter/area 整合）|
| 10 | **story** シーン | — | 未 | 🔵 別系統 | 22_ストーリー進行（キャラ定義・整合）|

---

## 3. 個別の設計メモ（実装時の要点）

### 🔴 #2 skills（スキル）— 最優先

**理由**: 最も多く参照される（`enemies.necromance.skillIds` / `jobs.skills` / 味方魔物）。
かつ **設計書 19 が「数値の憲法」として cost 帯ごとの power 倍率表を既に定義済み**なので、
決定論的ゲートが即座に作れる（ROI 最大）。

- スキーマ: `id, name, mpCost, power, type(PHYSICAL/MAGICAL), element, attackType, targetType, effectKey, description`
- バリデータ:
  - `power` を **19§2.2 の分類×コスト帯テーブル**に照合（範囲外 FAIL/WARN）
  - `type / element / attackType / targetType` 列挙チェック
  - `mpCost` を 19§3 のコスト設計式と照合
  - `effectKey` の命名規約、`id` 重複/snake_case
- プロンプト: 既存スキルの power 分布 + 分類テーブルを提示し、インフレ/デフレを防ぐ
- 連携候補: 既存サブエージェント **`balance-designer`** の知識と重複 → 統合検討

### 🔴 #3 demonForms（魔神化）— 高

**理由**: ゲームの核心機能。既に **`demonization-designer` サブエージェント**が存在する（Tier設計ルールを持つ）。
これを Gemini ベースの管理画面フローに移植/接続する形が自然。

- スキーマ: `jobId, formName, tier(1/2), concept, effectA{statBoosts,flags}, effectB{riskType,onAttackEffect}, ultimateSkill{damage{power,element,targetType,attackType,flags}, lingering{type,duration}}, visual`
- バリデータ:
  - `jobId` → jobs.json 参照（FAIL）
  - `tier ∈ {1,2}`、`ultimateSkill.damage.power` を **16§3 の魔神技 power 基準**に照合
  - `statBoosts` の倍率レンジ（過剰インフレ WARN）、`flags / onAttackEffect / lingering.type` 列挙
  - 同 jobId × tier の重複チェック
- 注意: Tier1=直感的な強さ / Tier2=リスク&リワード（悪魔合体）の設計ルール（doc16）を踏襲

### 🟡 #4 stages（ステージ）— 中（複合・参照重い）

**理由**: 第2章エリア構築に必須だが、複合オブジェクトで参照整合が多い。

- スキーマ: `id, name, chapter, area, nodeType, element, difficulty, waveCount, areaGimmick, unlockRequires[], waves[]{enemyIds[]}, rewards{baseExp,baseGold,dropTable[]}, position`
- バリデータ:
  - `waves[].enemyIds` → enemies.json 参照（FAIL）
  - `unlockRequires` → 他 stage 参照（FAIL）、循環依存チェック
  - `nodeType / areaGimmick / element` 列挙、`difficulty` と報酬カーブの整合（章進行）
  - `rewards.dropTable` の itemId 参照 + rate 0〜1
- 連携: 95_エリアマップ管理とステージ反映設計 の制約を踏襲

### 🟡 #5 monsters（味方魔物）— 中

**理由**: プレイヤー所持の魔物ロスター。enemies の味方化検証ロジックをほぼ流用できる。

- スキーマ: `name, tribe, cost, stats, resistances`（+ 編成で skills/spiritCore 等）
- バリデータ: `tribe` 列挙、`cost` と stats の釣り合い、stat スケール、resistances スケール（enemy 検証を再利用）

### 🟡 #6 items（武器）— 中

- スキーマ: `id, name, type, rarity/weaponRarity, archetype, rank, ilv, isUnique, stats, subOptions[]{type,value}, passiveA/B{nameJa,descTemplate,values[]}, flavor`
- バリデータ:
  - `rarity / archetype / subOption.type` 列挙
  - `passive.values[]` の長さ（rank 段階数）と rarity 別レンジ
  - `descTemplate` の `{value}` プレースホルダ整合、`id` 重複
- 参照: enemies.dropTable から参照される（被参照側）

### 🟢 #7 materials（素材）— 低（簡単・早い勝ち）

- スキーマ: `id, name, quantity, expValue, rarity`
- バリデータ: `rarity` 列挙、`expValue` 帯、`id` 重複。最小工数で 1 つ追加できる。

### 🟢 #8 jobs（職業）— 低頻度・複合

- スキーマ: `name, displayName, nameEn, title, tier, category, baseAttackType, role, description, statModifiers, energyCurve, levelBonuses, skills[]{level,skillId}`
- バリデータ: `category / baseAttackType` 列挙、`statModifiers` は 1.0 前後の倍率レンジ、`skills[].skillId` 参照、`tier`。
- 注: 12職業は概ね固定。新規追加頻度は低いが、追加時の整合確認には有用。

### 🟢 #9 areas（エリア）— 低（メタデータ）

- スキーマ: `id, chapter, area, nameJa, nameEn, description, color, position, sortOrder`
- バリデータ: `chapter/area` の一意性・連番、`color` HEX、`position` 範囲。

### 🔵 #10 story（ストーリーシーン）— 別系統

- 100 の提案 §3.4「ストーリーシーン生成フロー」/「Agent C」に相当。
- 検証は narrative 整合（キャラ口調・固有名詞・前後シーン）で、数値ゲートとは性質が異なる。
- 既存サブエージェント **`narrative-director`** の知識を活用。dialogue 配列の複数案生成 + 人間選択。

---

## 3.5 実装済 — #2 skills（スキル）✅（2026-06-08）

スキルは職業（または魔物）に紐づくため、**紐付き先（owner）を必須コンテキスト**とし、
その系統に合うスキルを生成・検閲する。

### ファイル

| ファイル | 役割 |
|---|---|
| `src/lib/agent/skillBalance.ts` | 決定論的バリデータ。設計書19の **POWER_TABLE**（cost帯×分類×Tier）+ owner-fit |
| `src/lib/agent/skillAgent.ts` | LangGraph。owner を必須に、power 表の該当範囲をプロンプトへ提示して生成→検証→再生成 |
| `src/app/admin/agents/actions.ts` | `generateSkillDraftAction(requirements, owner)` / `getSkillOwnerOptions()` |
| `src/components/admin/AISkillDraftPanel.tsx` | 紐付き先セレクタ（職業/魔物）付きパネル |
| `src/components/admin/forms/SkillForm.tsx` | isNew 時にパネル表示・草稿を流し込み |

### owner-fit 検閲（ユーザー要望）

- **type === 職業 category**（剣士=PHYSICAL に魔法スキルは **FAIL**）
- **attackType === 職業 baseAttackType**（揃わなければ **WARN**）
- **power は職業 Tier の範囲**（設計書19 §2.2 の表で Tier1/Tier2 を切替）
- 魔物 owner: element が属性傾向（resistances から導出）と不一致なら **WARN**
- effectKey は `element_attackType` 規約（不一致 WARN）、mpCost は分類別 cost 帯（物理4+/魔法8+）

### 検証・テスト

| 種別 | 結果 |
|---|---|
| 型チェック | PASS |
| ユニット（`skillBalance.test.ts`） | 16 件 PASS（power表/owner-fit/列挙/effectKey/id重複） |
| 全体回帰 | 47 suites / 359 tests PASS |
| ライブ（`npm run agent:verify-skill`） | mage で氷の全体魔法 → MAGICAL/MAGIC/ALL_ENEMIES・power 1.4（MAGIC_AOE低帯Tier1範囲内）・PASS |
| UI | `next dev` → `/admin/skills/new` HTTP 200・パネル描画確認 |

> 補足: 物理職(warrior)のライブ確認は Gemini 無料枠の日次上限到達で保留したが、
> type 不一致 FAIL / attackType WARN は同一コードでユニットテスト済み。
> 既知の別件: SkillForm の attackType ドロップダウン候補（PIERCE/BLUNT 等）が実データ
> （STRIKE/PROJECTILE/SUMMON）と乖離。バリデータは実データ列挙に準拠。フォーム候補の是正は別タスク。

---

## 3.6 実装済 — #3 demonForms（魔神化）✅（2026-06-08）

魔神化フォームは職業に紐づく（key=jobId）。**紐付き先職業の tier / 基礎攻撃**を必須コンテキストとし、
設計書16 の Tier 設計ルールに沿って生成・検閲する。

### ファイル

| ファイル | 役割 |
|---|---|
| `src/lib/agent/demonBalance.ts` | 決定論的バリデータ。Tier ルール + 魔神技 power 基準 + 列挙 |
| `src/lib/agent/demonAgent.ts` | LangGraph。職業 owner 必須、Tier 別ルールをプロンプト提示 |
| `src/app/admin/agents/actions.ts` | `generateDemonFormDraftAction(requirements, jobId)` / `getDemonJobOptions()` |
| `src/components/admin/AIDemonFormDraftPanel.tsx` | 職業セレクタ付きパネル |
| `src/components/admin/forms/DemonFormEditor.tsx` | isNew 時にパネル表示・草稿流し込み |

### Tier ルール検閲（設計書16 §3）

- **Tier1**: riskType は null 推奨（リスクなし=直感的な強さ）。set されていれば **WARN**。
- **Tier2**: riskType **必須**（null は **FAIL**）+ 魔神技 lingering（持続効果）**必須**（無ければ **FAIL**）+ riskValue 推奨。
- 魔神技 power は Tier 基準帯（Tier1 2.8〜3.6 / Tier2 3.6〜4.8）外で **WARN**。
- statBoosts は倍率表記（+80%=0.8）。パーセント整数(80)は **FAIL**（スケール誤り）。
- jobId 参照（FAIL）、tier と職業 tier 不一致（WARN）、ult.attackType と職業基礎攻撃の整合（WARN）。
- 列挙: element / target(SINGLE|**ALL**) / attackType / riskType / lingering.type（game.ts と同期）。

### 検証・テスト

| 種別 | 結果 |
|---|---|
| 型チェック | PASS |
| ユニット（`demonBalance.test.ts`） | 15 件 PASS（Tierルール/power基準/列挙/scale/参照） |
| agent ユニット合計 | 79 件 PASS |
| ライブ（Vertex `npm run agent:verify-demon`） | Tier2 archmage（MP過剰消費リスク・全体魔法）→ 1回FAIL→thinking 2048で再生成→PASS。Tier1 rogue（リスクなし単体）→ 1回 PASS |
| UI | `/admin/demon-forms/new` HTTP 200・パネル描画確認 |

---

## 3.7 実装済 — #4 stages（ステージ）✅（2026-06-08）

ステージはエリア（chapter/area）に紐づき、waves に敵を配置する複合データ。紐付き先エリアと
**敵カタログ（tier/種族/弱点）**・武器/素材・既存ステージ（解放条件にできる）を渡し、参照整合の
取れたステージを生成する。

### ファイル

| ファイル | 役割 |
|---|---|
| `src/lib/agent/stageBalance.ts` | 決定論的バリデータ。参照整合 + 構造 + 列挙 + 進行/報酬カーブ |
| `src/lib/agent/stageAgent.ts` | LangGraph。エリア owner + 敵カタログを提示して生成 |
| `src/app/admin/agents/actions.ts` | `generateStageDraftAction(requirements, areaId)` / `getStageAreaOptions()` |
| `src/components/admin/AIStageDraftPanel.tsx` | エリアセレクタ付きパネル |
| `src/components/admin/forms/StageForm.tsx` | isNew 時にパネル表示・草稿流し込み |

### 参照整合の検閲（複合・最重要）

- `waves[].enemyIds` → enemies.json（FAIL）。BOSS ノードは BOSS tier 内包を WARN。
- `unlockRequires` → 既存ステージ（FAIL）、自己参照 FAIL。
- chapter+area → areas.json（`ch{chapter}_area{area}`）（FAIL）。
- `rewards.dropTable.itemId` → WEAPON/CONSUMABLE=items.json / MATERIAL=materials.json（FAIL）、rate 0〜1。
- `waveCount === waves.length`（FAIL）、SAFE は wave 不要・戦闘ノードは wave 必須。
- 列挙: nodeType / element / wave.role / areaGimmick / drop.type（game.ts と同期）。
- 報酬カーブ: baseExp/baseGold が既存レンジから大きく外れたら WARN。

### 検証・テスト

| 種別 | 結果 |
|---|---|
| 型チェック | PASS |
| ユニット（`stageBalance.test.ts`） | 16 件 PASS（参照/waveCount/BOSS内包/列挙/report band） |
| agent ユニット合計 | 95 件 PASS / 全体 49 suites・390 tests PASS |
| ライブ（Vertex `npm run agent:verify-stage`） | ch1_area1 への続きダンジョン → unlockRequires=area1_node3・WAVE3にBOSS tier配置・diff5・全参照整合 PASS（1試行）|
| UI | `/admin/stages/new` HTTP 200・パネル描画確認 |

---

## 3.8 実装済 — #6 items（武器）✅（2026-06-08）

武器はレアリティが構成（サブオプション枠・パッシブ複雑度）を決めるため、**対象レアリティ**を
必須コンテキストとし、設計書13/92 のルールに沿って生成・検閲する。

### ファイル

| ファイル | 役割 |
|---|---|
| `src/lib/agent/weaponBalance.ts` | 決定論的バリデータ。レアリティ別 subOption 枠 + パッシブ 2スロット + 列挙 |
| `src/lib/agent/weaponAgent.ts` | LangGraph。レアリティ別ルール + power 表ならぬ枠構成をプロンプト提示 |
| `src/app/admin/agents/actions.ts` | `generateWeaponDraftAction(requirements, rarity)` |
| `src/components/admin/AIWeaponDraftPanel.tsx` | レアリティセレクタ付きパネル |
| `src/components/admin/forms/ItemForm.tsx` | isNew 時にパネル表示・草稿流し込み |

### レアリティ別 subOption 枠の検閲（設計書92・最重要）

| レアリティ | 通常枠 | 固定属性枠 |
|---|---:|---:|
| R / SR | 1 | 0 |
| SSR / UR | 1 | 1 |

- 枠数・枠種別（通常 vs 属性 DMG_BOOST）が一致しないと **FAIL**。
- subOption.value のスケール（>100 で FAIL / <1 で WARN）。
- passiveA/B 必須（無ければ FAIL）、descTemplate に `{value}`（無ければ WARN）、values 5要素（外れたら WARN）、systemTag 列挙。
- rarity と指定一致（FAIL）、UR は MYTHIC 推奨（WARN）、rank 1〜5、id 重複（FAIL）。

### 検証・テスト

| 種別 | 結果 |
|---|---|
| 型チェック | PASS |
| ユニット（`weaponBalance.test.ts`） | 18 件 PASS（枠構成/passive/scale/rarity） |
| agent ユニット合計 | 113 件 PASS / 全体 50 suites・408 tests PASS |
| ライブ（Vertex `npm run agent:verify-weapon`） | SSR（闇魔法剣）→ 通常枠CRIT_DMG+属性枠DARK_DMG_BOOST・DEMON_MODEパッシブ・PASS。R（物理片手剣）→ 通常枠ATK%のみ・PASS |
| UI | `/admin/items/new` HTTP 200・パネル描画確認 |

---

## 4. 推奨実装順

1. **skills** 🔴 — doc19 の表で決定論的ゲートが即作れる。被参照の中心。
2. **demonForms** 🔴 — 核心機能 + 既存 demonization-designer 連携。
3. **materials** 🟢 — 簡単。共通基盤（Server Action + パネル）の横展開テンプレ確立に好適。
4. **items** 🟡 → **monsters** 🟡 → **stages** 🟡
5. **jobs / areas** 🟢（必要時）
6. **story** 🔵（narrative 系として別途）

> 共通化の機会: `AIEnemyDraftPanel` を汎用 `AIDraftPanel<T>` に一般化し、
> 各コンテンツは「プロンプト構築 + バリデータ」だけ差し替える構成にすると追加コストが下がる。

---

## 5. 既存サブエージェントとの関係

プロジェクトには設計知識を持つサブエージェントが既にある（`balance-designer` / `demonization-designer` /
`economy-designer` / `narrative-director` 等）。これらは「設計知識」、本ロードマップの管理画面エージェントは
「決定論的ゲート + フォーム流し込み + 永続化」を担う。両者は競合せず、
**サブエージェントの設計ルールをバリデータ/プロンプトに落とし込む**形で接続するのが望ましい。
