# スマホレイアウト監査・修正概要（2026-07-27）

## 目的

スマホ操作を主対象として、主要画面のレイアウト崩れ、文字の見切れ、縦スクロール不能、タップ領域不足を確認し、再発防止を含めて修正する。

## 監査条件

- 基準表示領域: `390 x 844 CSS px`（一般的なスマホ縦持ち）
- 補助表示領域: `375 x 667 CSS px`（小型スマホ縦持ち）
- データ条件: `devPreset=endgame`（全ナビ・装備・残滓・黄泉を表示可能な状態）
- 対象: 拠点、ワールドマップ、転職、編成、統合詳細ハブ、武器庫、ネクロラボ、黄泉、ログ、戦闘導線
- 確認方法: 実ブラウザ巡回、DOM実寸計測、横方向オーバーフロー／非スクロール領域の検査、既存Playwrightシナリオ確認

## 検出事項と修正方針

| ID | 重要度 | 対象 | 確認結果 | 修正概要 |
|---|---|---|---|---|
| MOB-01 | Critical | 全画面 | Tailwind v4の間隔トークン `--spacing` が未定義で、`h-14`、`min-h-11`、`px-*`、`py-*` が計算不能。下部ナビは想定56pxに対して実測28.5px、黄泉の操作ボタンは想定44pxに対して実測19pxだった。 | 共通CSSにv4互換の間隔トークンを定義し、既存ユーティリティの意図した寸法を復元する。E2Eで共通ナビと主要操作の44px以上を検証する。 |
| MOB-02 | High | 拠点ヘッダー | 認証補助文言 `Next runtimeで認証有効` が112px枠で120px必要となり末尾が見切れる。ログインボタン34px、設定ボタン40px。 | ローカル実行時の補助文言を短く明確な表現へ変更し、認証・設定操作を44px以上へ拡張する。 |
| MOB-03 | High | 統合詳細ハブ | 残滓スロット内でアイコン・名前・Lvバッジが競合し、名前と主効果の実幅が2pxまで圧縮される。武器名も48px枠で見切れる。 | 狭幅カードから独立Lvバッジを除き、Lvを副情報行へ統合する。部位名を先頭に出し、名前領域を確保する。 |
| MOB-04 | Medium | ネクロラボ | 装備スロットの残滓名が72px一行省略となり、全件が同じ `深淵の残滓…` に見えて区別できない。 | 部位名＋接頭辞を除いた短縮名を優先表示し、フル名称はアクセシブル名／titleに保持する。 |
| MOB-05 | High | MAP・転職・編成・研究所・ログ | 戻る／ホーム操作が実測17～36px。片手操作で誤タップしやすい。 | 各ヘッダーの主要戻る操作を44px以上に統一し、安全領域を維持する。 |
| MOB-06 | High | 黄泉 | 各階の「再挑戦」が実測48 x 19px、「現在地へ」が高さ19px。 | 共通間隔トークン復元に加え、インラインの最小高さ44pxを付与してCSS生成差異にも耐える。 |
| MOB-07 | High | ログ | 「RETURN TO HUB」が高さ19px、物語／戦歴／世界タブが高さ16px、再生ボタンが42px。 | 戻る・タブ・再生操作を44px以上へ統一し、一覧のスクロール領域は `flex-1 min-h-0` のまま維持する。 |
| MOB-08 | Medium | 編成一覧 | DETAIL、COST、SYNERGYヘッダー、陣形カードが17～35pxの小さい操作領域。 | 主要トグルと詳細遷移に44pxの最小高さを設定。情報密度は保ちつつタップ領域だけ拡張する。 |
| MOB-09 | Medium | 戦闘・武器庫 | 武器庫は縦スクロール可能で、攻撃力・サブオプション・レア度を表示できる。戦闘は既存モバイルE2Eに到達性検査があるが、共通寸法退行は未検査。 | 既存武器・戦闘E2Eを再実行し、新しいモバイル共通レイアウトE2Eを追加する。 |
| MOB-10 | High | 戦闘HUD | 実戦到達後のAUTOが高さ38px、速度切替が31px、撤退が38pxで、戦闘中の片手操作にはタップ領域が不足する。 | システムバーのAUTO・速度切替・撤退を44px以上に統一し、戦闘中の横幅とHUD到達性をE2Eで検証する。 |

## 非不具合として扱う項目

- 転職画面の横方向ジョブレールは、意図した横スクロール領域のためページ横溢れには含めない。
- MAPの装飾SVG／パーティクルが画面外へ描画される箇所は、操作DOMと文書幅を増加させないため対象外とする。
- 一行省略自体は、別の識別情報が残り、フル名称をアクセシブル名で取得できる場合のみ許容する。

## 完了条件

1. 390px幅で文書全体の意図しない横スクロールが発生しない。
2. 拠点、MAP、転職、編成、統合詳細、武器庫、研究所、黄泉、ログで主要情報が判別可能である。
3. 主要な戻る・タブ・決定・再挑戦・下部ナビのタップ領域が44px以上である。
4. 武器庫と縦一覧がスマホでスクロール可能である。
5. TypeScript、Jest、Playwrightの関連テストが成功する。

## 実施結果

- MOB-01〜MOB-10を修正済み。
- `390 x 844` と `375 x 667` の両方で、対象画面の文書横溢れ、意図しない横方向の文字クリップ、安定表示後44px未満の操作ボタンが0件。
- 統合詳細ハブでは残滓を部位＋短縮名で判別でき、武器庫ではレア度・WEAPON ATK・サブオプションを維持したまま縦スクロール可能。
- 戦闘HUDのAUTO、速度切替、撤退を44px以上へ統一。
- 検証結果: `npx tsc --noEmit` 成功、E2E用TypeScript成功、Jest 92スイート／815件成功、関連Playwright 13件成功、production build成功。
