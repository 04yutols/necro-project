# Ch1 Phase 4 — 物語 実装証跡

作成: 2026-06-28
対象計画: [`CH1_PHASE4_物語_設計実装計画.md`](./CH1_PHASE4_物語_設計実装計画.md)

## 実装内容

- `src/data/story/ch1_scenes.json` に Ch1 中盤の2シーンを追加。
  - `CH1_DEADCITY_ENTRY`: `STAGE_CLEAR area1_a2`、sequence `13.5`、archiveTitle `死都への入城`
  - `CH1_FALLEN_TRUTH`: `STAGE_CLEAR area1_b3`、sequence `15.5`、archiveTitle `亡国の真実`
- 既存シーンのトリガー再配分は実施せず、`CH1_NODE1_AFTER` → 追加1 → `CH1_NODE2_ENTER`、`CH1_NODE2_AFTER` → 追加2 → `CH1_BOSS_ENTER` の順序だけ補完。
- `StoryRegistry.test.ts` / `packs.test.ts` / `tests/helpers/e2e.ts` の期待値を Ch1 15シーン、ch1 pack 19シーンへ更新。
- `docs/progress/CH1_TODO.md` / `GAME_LOOP_STATUS.md` / `docs/NEXT_ACTIONS.md` の現在シーン数を更新。

## 登録確認

```text
area1_a2 => CH1_DEADCITY_ENTRY
area1_b3 => CH1_FALLEN_TRUTH
total=23
ch1=15
```

## storyValidator

```text
CH1_DEADCITY_ENTRY: PASS / findings=5 / fails=0
CH1_FALLEN_TRUTH: PASS / findings=5 / fails=0
```

## テスト結果

```text
npm test -- --runTestsByPath src/data/story/StoryRegistry.test.ts src/data/story/packs.test.ts src/lib/agent/story/storyValidator.test.ts --runInBand
PASS: 3 suites / 28 tests
```

```text
npx tsc --noEmit
PASS
```

```text
npm run data:audit
Summary: 0 fail(s), 13 warn(s).
```

監査 WARN は既存の enemy/stage/job バランス警告で、今回追加した story JSON 由来の FAIL はなし。

```text
git diff --check
PASS
```

## 備考

- エンジン改修なし。変更は story JSON、登録回帰テスト、E2E補助の既読シーン一覧、進捗ドキュメントのみ。
- 作業開始時点で `src/components/tutorial/BubbleHint.tsx` に既存の未コミット変更あり。今回の物語実装では触っていない。
