# Ch1 Phase 5 — 最終監査 証跡

作成: 2026-06-28  
対象: `docs/progress/CH1_PHASE5_最終監査_チェックリスト.md`  
実行ブランチ: `feature/20260628` / HEAD `25d5b849 phase4`

## 判定

**リリース判定: 保留。**

自動ゲート、マスターデータ監査、バランス進行、対象回帰テストは通過。  
ただし Playwright 全体が `21 passed / 7 failed` で未green、かつ実機 iOS Safari 通し確認はこの環境では未実施。

## 1. 自動ゲート

| 項目 | 結果 | 証跡 |
|---|---:|---|
| `npx tsc --noEmit` | PASS | exit 0 |
| `npm test -- --runInBand --testPathIgnorePatterns=/src/tests/` | PASS | 84 suites / 780 tests |
| `git diff --exit-code src/data/master` | PASS | exit 0 |
| `git diff --check` | PASS | exit 0 |
| `npm run build` | PASS | Next.js build completed / 9 static pages generated |

補足: Jest はチェックリスト注記どおり DB 統合テストを含む `src/tests/` を除外。

## 2. データ監査

| 項目 | 結果 | 証跡 |
|---|---:|---|
| `npm run data:audit -- --strict` | PASS | 0 fail / 13 warn |
| `/admin/audit` 相当 `runMasterDataAudit()` | PASS | 352 pass / 17 warn / 0 fail |
| G1 解放グラフ | PASS | 到達性 OK: 13 stages / roots 2、循環なし |
| G2 コード参照 | PASS | `area1_node1` / `area1_a2` / `area1_a_mini` / `area1_node3` すべて OK |
| storyValidator 新2シーン | PASS | `CH1_DEADCITY_ENTRY` / `CH1_FALLEN_TRUTH`: fail 0 / warn 0 / pass 5 |

WARN は既存のレンジ・表示警告のみ:

- `data:audit`: `grave_knight` shieldなし、既存 SHIELD/BOSS wave 表示警告、Tier2職業スコア警告。
- `/admin/audit`: 旧 `monsters.json` の key-only id 警告、Tier2職業スコア警告。

## 3. バランス

| コマンド | 結果 |
|---|---:|
| `npm run balance:report -- --all --strict` | PASS exit 0 / warning 1 |
| `npm run balance:progression -- --all --strict` | PASS / progression cliff warning 0 |

重点ノード:

| stage | EHP | peakTHR | 判断 |
|---|---:|---:|---|
| `area1_a_mini` | 237 | 10.18 | 1-4ミニボス山場として許容 |
| `area1_c2` | 295 | 13.61 | c1比 EHP 0.99x / peakTHR 0.91x |
| `area1_c3` | 426 | 17.20 | c2比 EHP 1.44x / peakTHR 1.26x、pre-bossとして過剰警告なし |

既存 WARN: `area1_node2` の BOSS WAVE 脅威度が直前WAVE未満。

## 4. 進行・UI

対象回帰テスト:

```text
npm test -- --runInBand \
  src/data/tutorial/phases.test.ts \
  src/data/tutorial/triggers.test.ts \
  src/hooks/useTutorialTrigger.test.ts \
  src/store/useTutorialStore.test.ts \
  src/data/stageRefs.test.ts \
  src/data/ch1Phase2NewEnemies.test.ts \
  src/data/story/StoryRegistry.test.ts \
  src/data/story/packs.test.ts \
  src/lib/agent/story/storyValidator.test.ts \
  src/lib/agent/stageGraph.test.ts
```

結果: PASS 10 suites / 68 tests。

静的確認:

- Ch1 sortOrder: `area1_safe -> area1_node1 -> area1_a2 -> area1_a_mini -> area1_node2 -> area1_b2 -> area1_b3 -> area1_c1 -> area1_c2 -> area1_c3 -> area1_boss -> area1_node3`
- `area1_boss` は difficulty 3、c1-c3 は difficulty 4。進行順は sortOrder 駆動なので機能影響なし。
- `JOB_CHANGE` / `WEAPON_ENHANCE` は本番コード残存参照なし。Phase 3仕様どおり強制フェーズから削除され、BubbleHintに格下げ済み。

モバイルChromium確認:

| 対象 | 結果 |
|---|---|
| Ch1 AreaMap | `docs/progress/CH1_PHASE5_mobile_ch1_map_2026-06-28.png` / 390x844 |
| JOB BubbleHint | `docs/progress/CH1_PHASE5_mobile_job_hint_2026-06-28.png` / 390x844 |
| JOB BubbleHint bounding box | x 90.47 / y 468 / w 210 / h 164.75、viewport内 |

未実施:

- 実機 iOS Safari での通し確認。
- 1-1から残滓ノードまでの人手フル通しプレイ。

## 5. 新敵・物語・既知事項

新敵:

- `gravewarden_colossus`: BOSS / UNDEAD / `SUMMON_MINIONS` value 1 / `grave_soldier` 召喚解決テストあり。
- `wandering_guard_wraith`: MINION / UNDEAD / captureRate 0.12 / allyCost 1。
- `cursed_head_maid`: ELITE / HUMANOID / shieldHp 12 / captureRate 0.04 / allyCost 2。
- `dragonbone_spawn`: MINION / DRAGON / captureRate 0.12 / allyCost 1。

物語:

- `CH1_DEADCITY_ENTRY`: `STAGE_CLEAR area1_a2` / sequence 13.5。
- `CH1_FALLEN_TRUTH`: `STAGE_CLEAR area1_b3` / sequence 15.5。

既知事項:

- TECH_DEBT L-3 は `docs/progress/TECH_DEBT.md` に記録済み。
- Phase 3 BubbleHint 関連は `3026de83 phase3` に含まれる。
- 孤児 story ファイルは未ロード。`src/data/story/packs.ts` は `ch1_scenes.json` / `ch2_scenes.json` のみロード。
- チェックリスト記載ブランチ `feature/2026062102` と実行ブランチ `feature/20260628` が異なる。

## 6. Playwright

実行:

```text
PLAYWRIGHT_TEST_BASE_URL=http://localhost:3080 npx playwright test
```

結果: **FAIL: 21 passed / 7 failed**。

失敗内訳:

| spec | 理由 |
|---|---|
| `active-skills.spec.ts` 2件 | 期待値が旧MP `100` / `95`。現行表示は `MP10` / `MP5`。 |
| `active-skills.spec.ts` 1件 | 期待ログが旧「スケルトンの追撃」。現行ログは「骸骨騎士の攻撃」系。 |
| `auth-debug.spec.ts` 2件 | デバッグspecのクリック順確認がタイムアウト。 |
| `result-screen.spec.ts` 1件 | Next dev overlay `<nextjs-portal>` が `AUTO OFF` クリックを遮蔽。 |
| `new-player-onboarding.spec.ts` T-07 | 同上、Next dev overlay が `AUTO OFF` クリックを遮蔽。 |

Playwright report artifacts:

- `test-results/.last-run.json`
- `test-results/*/error-context.md`

## 最終メモ

§1〜§3と対象Jestはgreen。  
§4のモバイルChromium補助確認は一部OK。  
§6 Playwright全体と実機iOS Safariが未greenのため、Phase 5リリースゲートはまだ閉じない。
