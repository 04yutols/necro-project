# CH1 Phase 2 新規敵 実装証跡

> 実施日: 2026-06-28  
> 対象計画: `docs/progress/CH1_PHASE2_新規敵_設計実装計画.md`

## 実装概要

`skills.json` とエンジンには手を入れず、既存スキルと既存ギミックだけで第1章Phase 2の新規敵を実装した。

### 追加敵

| id | 表示名 | tier | tribe | 主な役割 |
|---|---|---|---|---|
| `gravewarden_colossus` | 墓守の巨像 | BOSS | UNDEAD | 1-4ミニボス。防壁破壊で `SUMMON_MINIONS` |
| `wandering_guard_wraith` | 彷徨う近衛霊 | MINION | UNDEAD | 王城後半の攻撃寄りミニオン |
| `cursed_head_maid` | 呪詛の女官長 | ELITE | HUMANOID | 1-10上位術師。薄い防壁つき |
| `dragonbone_spawn` | 竜骨の眷属 | MINION | DRAGON | 祭壇ゾーンのDRAGON捕獲導入 |

全4体で tier 別ネクロマンス規約を適用:

| tier | captureRate | allyCost |
|---|---:|---:|
| MINION | 0.12 | 1 |
| ELITE | 0.04 | 2 |
| BOSS | 0.001 | 4 |

## ステージ差し替え

| ステージ | wave | 変更後 |
|---|---|---|
| `area1_a_mini` | W3 | `gravewarden_colossus`, `grave_soldier`。roleを `BOSS` に変更 |
| `area1_b3` | W3 | `wandering_guard_wraith`, `bone_colossus` |
| `area1_c1` | W1 | `wandering_guard_wraith`, `dragonbone_spawn` |
| `area1_c1` | W3 | `bone_colossus`, `dragonbone_spawn`, `wandering_guard_wraith` |
| `area1_c2` | W3 | `cursed_head_maid`, `abyss_warden`, `bone_colossus` |
| `area1_c3` | W1 | `dragonbone_spawn`, `bloodmire_leech`, `earthbound_grudge` |
| `area1_c3` | W2 | `abyss_warden`, `dragonbone_spawn` |
| `area1_c3` | W3 | `blood_mire_queen` 維持 |

既存 `statScale` と報酬テーブルは維持した。

## 追加テスト

`src/data/ch1Phase2NewEnemies.test.ts` を追加し、次を固定した。

- 4体の追加と tier / tribe / ネクロマンス規約
- `validateEnemyDraft` が4体すべて WARN / FAIL なし
- `gravewarden_colossus` の `SUMMON_MINIONS` が `grave_soldier` 1体へ解決されること
- 計画書どおりの5ステージ差し替え
- `validateStageDraft` が差し替え対象5ステージすべて FAIL なし

## 検証結果

| コマンド | 結果 |
|---|---|
| `npm test -- --runTestsByPath src/data/ch1Phase2NewEnemies.test.ts --runInBand` | PASS: 1 suite / 5 tests |
| `npm run data:audit -- --strict` | PASS: 0 fail / 13 warn |
| `NODE_ENV=development npx tsx -e "...auditMasterData()"` | PASS: `/admin/audit`相当 FAIL 0 / WARN 17 |
| `npx tsc --noEmit` | PASS |
| `git diff --check` | PASS |
| `npm run balance:report -- --all --strict` | PASS: exit 0 / warning 1 |
| `npm run balance:progression -- --all --strict` | PASS: progression cliff warning 0 |
| `npm test -- --runInBand` | PASS: 88 suites / 785 tests |

`data:audit` のWARNは既存の `grave_knight` shieldなし、既存SHIELD wave警告、Tier2職業スコア警告。
`balance:report` のWARNは既存の `area1_node2` BOSS WAVE脅威度警告。
今回追加した4体と差し替え対象5ステージは、専用Jest内の `enemyBalance` / `validateStageDraft` でWARN / FAILなしを確認した。

## バランス証跡

`npm run balance:progression -- --all --strict` では隣接ステージの cliff warning は0。
主な対象ノードのピーク値:

| stage | EHP | peakTHR | 判断 |
|---|---:|---:|---|
| `area1_a_mini` | 237 | 10.18 | `area1_a2` から EHP 1.87x / peakTHR 1.89x。ミニボス山場として許容 |
| `area1_c2` | 295 | 13.61 | `area1_c1` から EHP 0.99x / peakTHR 0.91x。1-10術師スパイクはW3内で表現 |
| `area1_c3` | 426 | 17.20 | `area1_c2` から EHP 1.44x / peakTHR 1.26x。女王維持で終盤圧を維持 |

## 未実施

実機・ブラウザでの手動通しプレイは未実施。今回の証跡はマスターデータ監査、バリデータ、Jest、バランスレポートによる自動検証。
